datasource sqlite {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// --------------------------------------
model Link {
  id          Int     @default(autoincrement()) @id
  url         String
  name        String
  description String?
}
